Hello. The data in data/clean is committed to git (it is relatively small). So, to run the models, all you need to do is go to notebooks/ar/ar.ipynb and click through the chunks (and make some changes to make sure it runs instead of tries to load model folders that aren't committed). The model outputs should be saved, and then results/sta720-project-report can be fully rendered.
